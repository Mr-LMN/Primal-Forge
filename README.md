# Here are your Instructions

Note on builds: react-native-svg needs a native build (EAS build or dev client) — it won't render in plain Expo Go. If you're testing via Expo Go right now you'll need `npx expo start --dev-client` with a development build, but everything will work fine in production EAS builds.
